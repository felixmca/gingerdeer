import { DbOneOffPage } from "@/components/db-one-off-page";

export const metadata = { title: "One-off Orders — Juice for Teams" };

export default function OneOffOrdersPage() {
  return <DbOneOffPage />;
}
