import { DbSubscriptionPage } from "@/components/db-subscription-page";

export const metadata = { title: "Subscriptions — Juice for Teams" };

export default function SubscriptionsPage() {
  return <DbSubscriptionPage />;
}
